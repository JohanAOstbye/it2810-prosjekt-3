import {
 CHANGE_TERM
} from './types';

/**
 * Function for a user with the given userid to attend a course with the given courseid
 * @param {Object} param0 - { userid, courseid }
 */
export const changeTerm = (term) => dispatch => {
    dispatch({ type: CHANGE_TERM })
    {
        
    }
}